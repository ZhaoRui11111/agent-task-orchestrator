# EP-03D A0 attempt 2 — ready for activation

Reviewer: `/root/ep03d_a0_2`

Reviewed at: `2026-09-04 11:10:02+08:00`

Reviewed material base: `9790dc3d21eea7c2c0257ababc1d70fd1bcd6c33`

Approval contract: 19,885 canonical UTF-8 bytes, SHA-256 `FCB976AA3A6083438555B55C9A70CD2131BF2D8B6852B104B965C9BE473009B2`

Independence: fresh independent non-drafter, non-reviser, non-implementer, strictly read-only, non-fail-fast A0 reviewer. The reviewer did not participate in proposal drafting, attempt 1, or revision 1; did not open the superseded `ep-03d` task or its evidence; and did not edit files, Git, the ExecPlan, coordinator state, fixtures, network state, credentials, external Projects, cleanup, integration, push, release, deployment, Codex account state, scheduler/MCP state, or `D:\quant`. Historical metadata visible in the current proposal/trace was not used as a substitute for fresh evaluation.

Scope: the complete current proposal; repository authority; the ExecPlan schema, A0, and Tier-2 persistence lenses; relevant adapter, versioning, authorization, persistence, reliability, completion/workspace, toolchain, validation, privacy, threat, product, execution, and Codex feasibility owners and implementations; and current official OpenAI Codex SDK/App Server documentation.

Evidence: exactly one current trace exited zero with schema version 3, approval bytes 19,885, approval/current material base and HEAD/actual HEAD all equal to the reviewed base, and empty errors, warnings, outside-scope paths, overlap, and pre-existing dirty paths. Independent canonical sorted-key compact UTF-8 serialization reproduced 19,885 bytes and the exact digest. The goal/non-goals, scope, six milestones, fourteen binary validations, recovery, state bindings, and risk controls close without an implicit Scheduler, MCP, App Server, CLI, platform-support, or real-account deliverable.

The reviewer independently confirmed that the current execution/v1 hard-codes `workspaceMode=none` and `local-manual/v1`; D2/D3 and V3–V5 specify one discriminated execution/v2 where retained Manual uses `none` and Codex requires the exact owned `ato.workspace/v2` context. Current authorization vocabulary 6 does not name Codex, a credential destination, or general network authority; C4/C9, D4/D9, V8/V9, and R7 therefore keep every Codex constructor, configuration, driver, and selector outside the supported package root and all current production factories, leaving operational authority Manual-only. V2 freezes a hard gate for the exact pinned stable SDK, readable durable new-thread identity timing, and awaited non-model-text turn-terminal signal; V6 and R2 bind the implementation to that evidence and require a stop without App Server fallback if the package cannot prove it. V14/M6 now close only the pre-terminal exact-state audit and validation lifecycle; the separate completed-plan commit, Git-flow gates, FF-only integration, and ordinary push occur afterward.

The Tier-2 persistence lens is appropriate for durable thread/turn linkage and restart/resume. The contract keeps one writer/reader closure, intent-before-effect ordering, calls outside writer transactions, ordered observation/verification/finalization, combined decode/digest/backup/restart validation, stale-fence refusal, and explicit ambiguity for unprovable state; it adds no second schema, writer, or reliability owner. Official documentation proves SDK start/continue/resume and App Server's deeper client role but does not prove the exact cwd, identity timing, cancel/inspect, or terminal signal, so the pinned-package preflight remains the truthful evidence boundary.

Parent disposition: accepted. The report is complete, `ready_for_activation`, bound to the exact approval digest and reviewed material base, and has no finding. This does not grant SDK download, registry audit, secrets, account execution, App Server, external Project, or any other network permission.

Lifecycle addendum at `2026-09-04 11:16:09+08:00`: the first active trace, which occurs only after activation, emitted `W_PREFLIGHT_LIFECYCLE_SCOPE` because the helper additionally requires the non-created singular `docs/plans/proposal/EP-03D-codex-execution-backend.md` ownership sentinel. The parent returned the plan to proposal and changed the attempt disposition to superseded. No conclusion about the reviewed 19,885-byte contract was reused for the revised digest; a fresh independent A0 is required.

## Findings

None.
